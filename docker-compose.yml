version: '3'

services:
  dingtalk-dify-adapter:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dingtalk-dify-adapter-stream
    restart: always
    volumes:
      - ./config.json:/app/config.json
      - ./logs:/app/logs
    environment:
      - TZ=Asia/Shanghai 